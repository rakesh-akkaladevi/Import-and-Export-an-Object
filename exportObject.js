// Write an object with keys as `firstName` and `lastName` and values as `John` and
// `Wilson` respectively in the file
//  `exportObject.js` and export it using the default export syntax.

module.exports = name = {
  firstName: "John",
  lastName: "Wilson",
};
